.superpowers/
node_modules/

# Claude worktrees
.worktrees/

# Claude tooling — never publish
.claude/
.claude-plugin/

# Secrets — NEVER publish (contain sb_secret_ admin key / DB creds)
.env
*.env
enrich_disease_context.py
